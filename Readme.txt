1, User class: 
Store user info: id (auto-generated), name, email.

2, mainController:
Process requests from client including post and get.
post: add a new user into database.
get: fetch info from database.

3, make sure you set mysql well.
4, After application is started, use curl to send requests. 
